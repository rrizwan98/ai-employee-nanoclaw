"""
{{SYSTEM_NAME}} Multi-Agent System
Orchestrator with specialist agents using agent-as-tool pattern.
Generated by AI Employee using OpenAI Agents SDK + ChatKit
"""
import asyncio
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from pydantic import BaseModel
from typing import Optional, List
from agents import Runner, SQLiteSession
from agents.orchestrator import orchestrator_agent
from context.shared_context import shared_context
from config import settings
from server import server

app = FastAPI(
    title="{{SYSTEM_NAME}}",
    description="Multi-agent system for {{DOMAIN}} with orchestrator and specialist agents + ChatKit",
    version="1.0.0"
)

# CORS middleware
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)


# Mount ChatKit routes
app.include_router(server.router, prefix="/chatkit")


class SystemStatus(BaseModel):
    """System status model."""
    status: str
    system_name: str
    domain: str
    orchestrator: str
    specialists: List[str]
    active_sessions: int
    total_tasks: int


@app.get("/status", response_model=SystemStatus)
async def get_status():
    """
    Get system status and statistics.
    """
    tasks = shared_context.get("tasks", [])
    conversations = shared_context.get("conversations", [])

    return SystemStatus(
        status="healthy",
        system_name="{{SYSTEM_NAME}}",
        domain="{{DOMAIN}}",
        orchestrator="{{ORCHESTRATOR_NAME}}",
        specialists={{SPECIALIST_NAMES_LIST}},
        active_sessions=len(set(c.get("session_id", "default") for c in conversations)),
        total_tasks=len(tasks)
    )


@app.get("/tasks")
async def get_tasks(status: Optional[str] = None):
    """
    Get all tasks, optionally filtered by status.
    """
    tasks = shared_context.get("tasks", [])

    if status:
        tasks = [t for t in tasks if t.get("status") == status]

    return {"tasks": tasks}


@app.get("/activity")
async def get_activity(limit: int = 50):
    """
    Get recent activity log.
    """
    log = shared_context.get_activity_log(min(limit, 100))
    return {"activity": log}


@app.get("/health")
async def health():
    """Health check endpoint."""
    return {
        "status": "healthy",
        "system": "{{SYSTEM_NAME}}",
        "domain": "{{DOMAIN}}",
    }


@app.get("/")
async def root():
    """Root endpoint with system info."""
    return {
        "name": "{{SYSTEM_NAME}}",
        "domain": "{{DOMAIN}}",
        "orchestrator": {
            "name": "{{ORCHESTRATOR_NAME}}",
            "role": "Central coordinator that delegates to specialists"
        },
        "specialists": [
            {"name": "researcher_agent", "description": "Specialist for research tasks"},
            {"name": "writer_agent", "description": "Specialist for writing tasks"},
            {"name": "reviewer_agent", "description": "Specialist for review tasks"}
        ],
        "endpoints": {
            "chatkit": "/chatkit/*",
            "status": "GET /status",
            "tasks": "GET /tasks?status=<optional>",
            "activity": "GET /activity?limit=<optional>",
            "health": "GET /health"
        },
        "architecture": "Agent-as-tool pattern with shared context"
    }


if __name__ == "__main__":
    import uvicorn
    uvicorn.run(app, host="0.0.0.0", port=8000)
